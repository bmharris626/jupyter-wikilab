{
  "type": "object",
  "title": "WikiLab",
  "description": "WikiLab extension settings.",
  "jupyter.lab.setting-icon": "ui-components:notebook",
  "jupyter.lab.menus": {
    "context": [
      {
        "command": "jupyterhub-wikilab:open-sidebar",
        "selector": ".jp-DirListing-item[data-isdir=\"true\"]",
        "rank": 50
      }
    ]
  },
  "properties": {
    "committerEmail": {
      "title": "Committer Email",
      "description": "Email address used for wiki page commits. If empty, defaults to {username}@wikilab.",
      "type": "string",
      "default": "{username}@wikilab"
    },
    "defaultWikiPath": {
      "title": "Default Wiki Path",
      "description": "Default path where wiki repositories are stored. Used when registering a new wiki.",
      "type": "string",
      "default": "./wikis"
    }
  },
  "additionalProperties": false,
  "required": []
}
